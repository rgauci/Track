<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">@color/primary</item>
        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <!-- colorAccent is used as the default value for colorControlActivated,
             which is used to tint widgets -->
        <item name="colorAccent">@color/accent</item>
        <!-- primary text color -->
        <item name="android:textColorPrimary">@color/primary_text</item>
        <!-- secondary text color (e.g. underneath text) -->
        <item name="android:textColorSecondary">@color/secondary_text</item>
        <item name="background">@color/purple_500</item>
        <item name="android:textAppearanceMedium">@style/MediumText</item>
        <item name="android:textAppearanceLarge">@style/LargeText</item>
        <item name="android:textAppearanceSmall">@style/SmallText</item>
        <item name="actionMenuTextColor">@color/primary</item>
        <item name="android:dialogTheme">@style/DialogTheme</item>
        <item name="android:spinnerItemStyle">@style/TextViewSpinnerItem</item>
        <!-- You can also set colorControlNormal, colorControlActivated
             colorControlHighlight, and colorSwitchThumbNormal. -->
    </style>

    <style name="DialogTheme" parent="Base.Theme.AppCompat.Light.Dialog">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
    </style>

    <style name="TextViewSpinnerItem" parent="@android:style/Widget.TextView.SpinnerItem">
        <item name="android:paddingLeft">0dp</item>
        <item name="android:paddingRight">8dp</item>
    </style>

    <style name="Theme.AppCompat.Light.NoActionBar.FullScreen" parent="@style/Theme.AppCompat.Light">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="RaisedButtonColoured" parent="Widget.AppCompat.Button">
        <item name="colorButtonNormal">@color/primary</item>
        <item name="android:textColor">@color/text_icons</item>
        <item name="colorControlHighlight">@color/accent</item>
        <item name="colorControlActivated">@color/primary_dark</item>
    </style>

    <style name="RaisedButtonTransparent" parent="Widget.AppCompat.Button">
        <item name="colorButtonNormal">@color/background_floating_material_light</item>
    </style>

    <style name="RaisedButtonColoured_Next" parent="RaisedButtonColoured">
        <item name="android:drawableRight">@drawable/ic_ic_ic_navigate_next_white_large_24dp</item>
    </style>

    <style name="RaisedButtonColoured_Previous" parent="RaisedButtonColoured">
        <item name="android:drawableRight">@drawable/ic_ic_ic_navigate_before_white_large_24dp</item>
    </style>

    <style name="LikertButton" parent="Widget.AppCompat.Button.Small">
        <item name="android:layout_marginRight">@dimen/grid_vertical_margin</item>
        <item name="android:background">@color/background_light</item>
        <item name="android:textColor">@color/primary_text</item>
        <item name="android:layout_marginBottom">@dimen/horizontal_divider_height</item>
    </style>

    <style name="LikertButtonAnchor" parent="MediumText">
        <item name="android:textSize">@dimen/abc_text_size_button_material</item>
    </style>

    <style name="LargeText" parent="TextAppearance.AppCompat.Large">
    </style>

    <style name="MediumText" parent="TextAppearance.AppCompat.Medium">
        <item name="android:textColor">@color/primary_text</item>
    </style>

    <style name="SmallText" parent="TextAppearance.AppCompat.Small">
    </style>

    <style name="SubTitleText" parent="SmallText">
        <item name="android:textColor">@color/primary_light</item>
    </style>

    <style name="QuestionText" parent="MediumText">
        <item name="android:layout_marginBottom">@dimen/bottom_margin</item>
    </style>

    <style name="ErrorText" parent="TextAppearance.AppCompat.Small">
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">#DD2C00</item>
        <item name="android:visibility">gone</item>
    </style>

    <style name="ExplanationText" parent="android:style/TextAppearance.Small">
        <item name="android:layout_marginTop">16dp</item>
        <item name="android:layout_marginBottom">16dp</item>
    </style>

    <style name="HorizontalDivider">
        <item name="android:layout_marginTop">@dimen/horizontal_divider_vertical_margin</item>
        <item name="android:layout_marginBottom">@dimen/horizontal_divider_vertical_margin</item>
        <item name="android:layout_marginLeft">@dimen/activity_vertical_margin</item>
        <item name="android:background">@color/divider</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_height">@dimen/horizontal_divider_height</item>
    </style>

    <style name="TextInputLayout">
        <item name="android:minHeight">@dimen/floating_text_label_tile_height</item>
    </style>

    <style name="FormImage">
        <item name="android:height">72dp</item>
        <item name="android:layout_gravity">center</item>
    </style>

    <style name="ListItem">
        <item name="android:layout_marginLeft">@dimen/activity_vertical_margin</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:minHeight">@dimen/list_item_height</item>
    </style>

    <style name="BodyLayout">
        <item name="android:paddingLeft">@dimen/activity_horizontal_margin</item>
        <item name="android:paddingRight">@dimen/activity_horizontal_margin</item>
        <item name="android:paddingTop">@dimen/activity_vertical_margin</item>
        <item name="android:paddingBottom">@dimen/activity_vertical_margin</item>
    </style>

    <style name="Toolbar">
        <item name="android:minHeight">@dimen/title_height</item>
        <item name="android:background">?attr/colorPrimary</item>
        <item name="android:orientation">vertical</item>
        <item name="android:paddingTop">@dimen/activity_vertical_margin</item>
        <item name="android:paddingBottom">@dimen/activity_vertical_margin</item>
        <item name="android:paddingLeft">@dimen/activity_horizontal_margin</item>
    </style>

    <style name="FullscreenTheme" parent="android:Theme.NoTitleBar">
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@null</item>
        <item name="metaButtonBarStyle">@style/ButtonBar</item>
        <item name="metaButtonBarButtonStyle">@style/ButtonBarButton</item>
    </style>

    <!-- Backward-compatible version of ?android:attr/buttonBarStyle -->
    <style name="ButtonBar">
        <item name="android:paddingLeft">2dp</item>
        <item name="android:paddingTop">5dp</item>
        <item name="android:paddingRight">2dp</item>
        <item name="android:paddingBottom">0dp</item>
        <item name="android:background">@android:drawable/bottom_bar</item>
    </style>

    <!-- Backward-compatible version of ?android:attr/buttonBarButtonStyle -->
    <style name="ButtonBarButton" />

</resources>
